{
  "name": "logix-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently --kill-others-on-fail -n \"Frontend,Backend\" -c \"orange.bold,blue.bold\" \"pnpm --prefix frontend dev\" \"pnpm --prefix backend dev\"",
    "dev:fe": "pnpm --prefix frontend dev",
    "dev:be": "pnpm --prefix backend dev",
    "build:fe": "pnpm --prefix frontend build",
    "build:be": "pnpm --prefix backend build",
    "build": "pnpm run build:be && pnpm run build:fe"
  }
}
